/*-------------------------------------------------------
 * This file was generated by JParser
 *
 * Do not make changes to this file
 *-------------------------------------------------------*/

package gen.web.com.github.xpenatan.box3d;

import gen.web.com.github.xpenatan.jParser.api.NativeObject;

public final class B3Collision extends NativeObject {

    static private B3DistanceOutput B3DistanceOutput_TEMP_STATIC_GEN_0;

    static private B3CastOutput B3CastOutput_TEMP_STATIC_GEN_0;

    static private B3TOIOutput B3TOIOutput_TEMP_STATIC_GEN_0;

    static private B3PlaneSolverResult B3PlaneSolverResult_TEMP_STATIC_GEN_0;

    static private B3Vec3 B3Vec3_TEMP_STATIC_GEN_0;

    static public final B3Collision NULL = B3Collision.native_new();

    /**
     * Dummy constructor, used internally to creates objects without C++ pointer
     */
    @Deprecated()
    protected B3Collision(byte b, char c) {
    }

    /**
     * @return An empty instance without a native address
     */
    public static B3Collision native_new() {
        return new B3Collision((byte) 0, (char) 0);
    }

    protected void deleteNative() {
        internal_native_deleteNative(native_address);
    }

    @org.teavm.jso.JSBody(params = {"this_addr"}, script = "var jsObj = box3d.wrapPointer(this_addr, box3d.B3Collision);box3d.destroy(jsObj);")
    public static native void internal_native_deleteNative(int this_addr);

    public static B3LocalManifold CollideSpheres(int capacity, B3Sphere sphereA, B3Sphere sphereB, B3Transform transformBtoA) {
        int addr = internal_native_CollideSpheres_addr(capacity, sphereA.native_address, sphereB.native_address, transformBtoA.native_address);
        if (addr == 0)
            return B3LocalManifold.NULL;
        B3LocalManifold B3LocalManifold_NEW = B3LocalManifold.native_new();
        B3LocalManifold_NEW.internal_reset(addr, true);
        return B3LocalManifold_NEW;
    }

    @org.teavm.jso.JSBody(params = {"capacity", "sphereA_addr", "sphereB_addr", "transformBtoA_addr"}, script = "var returnedJSObj = box3d.B3Collision.prototype.CollideSpheres(capacity, sphereA_addr, sphereB_addr, transformBtoA_addr);if(!returnedJSObj.hasOwnProperty('ptr')) return 0; return box3d.getPointer(returnedJSObj);")
    public static native int internal_native_CollideSpheres_addr(int capacity, int sphereA_addr, int sphereB_addr, int transformBtoA_addr);

    public static B3LocalManifold CollideCapsuleAndSphere(int capacity, B3Capsule capsuleA, B3Sphere sphereB, B3Transform transformBtoA) {
        int addr = internal_native_CollideCapsuleAndSphere_addr(capacity, capsuleA.native_address, sphereB.native_address, transformBtoA.native_address);
        if (addr == 0)
            return B3LocalManifold.NULL;
        B3LocalManifold B3LocalManifold_NEW = B3LocalManifold.native_new();
        B3LocalManifold_NEW.internal_reset(addr, true);
        return B3LocalManifold_NEW;
    }

    @org.teavm.jso.JSBody(params = {"capacity", "capsuleA_addr", "sphereB_addr", "transformBtoA_addr"}, script = "var returnedJSObj = box3d.B3Collision.prototype.CollideCapsuleAndSphere(capacity, capsuleA_addr, sphereB_addr, transformBtoA_addr);if(!returnedJSObj.hasOwnProperty('ptr')) return 0; return box3d.getPointer(returnedJSObj);")
    public static native int internal_native_CollideCapsuleAndSphere_addr(int capacity, int capsuleA_addr, int sphereB_addr, int transformBtoA_addr);

    public static B3LocalManifold CollideHullAndSphere(int capacity, B3Hull hullA, B3Sphere sphereB, B3Transform transformBtoA) {
        int addr = internal_native_CollideHullAndSphere_addr(capacity, hullA.native_address, sphereB.native_address, transformBtoA.native_address);
        if (addr == 0)
            return B3LocalManifold.NULL;
        B3LocalManifold B3LocalManifold_NEW = B3LocalManifold.native_new();
        B3LocalManifold_NEW.internal_reset(addr, true);
        return B3LocalManifold_NEW;
    }

    @org.teavm.jso.JSBody(params = {"capacity", "hullA_addr", "sphereB_addr", "transformBtoA_addr"}, script = "var returnedJSObj = box3d.B3Collision.prototype.CollideHullAndSphere(capacity, hullA_addr, sphereB_addr, transformBtoA_addr);if(!returnedJSObj.hasOwnProperty('ptr')) return 0; return box3d.getPointer(returnedJSObj);")
    public static native int internal_native_CollideHullAndSphere_addr(int capacity, int hullA_addr, int sphereB_addr, int transformBtoA_addr);

    public static B3LocalManifold CollideCapsules(int capacity, B3Capsule capsuleA, B3Capsule capsuleB, B3Transform transformBtoA) {
        int addr = internal_native_CollideCapsules_addr(capacity, capsuleA.native_address, capsuleB.native_address, transformBtoA.native_address);
        if (addr == 0)
            return B3LocalManifold.NULL;
        B3LocalManifold B3LocalManifold_NEW = B3LocalManifold.native_new();
        B3LocalManifold_NEW.internal_reset(addr, true);
        return B3LocalManifold_NEW;
    }

    @org.teavm.jso.JSBody(params = {"capacity", "capsuleA_addr", "capsuleB_addr", "transformBtoA_addr"}, script = "var returnedJSObj = box3d.B3Collision.prototype.CollideCapsules(capacity, capsuleA_addr, capsuleB_addr, transformBtoA_addr);if(!returnedJSObj.hasOwnProperty('ptr')) return 0; return box3d.getPointer(returnedJSObj);")
    public static native int internal_native_CollideCapsules_addr(int capacity, int capsuleA_addr, int capsuleB_addr, int transformBtoA_addr);

    public static B3LocalManifold CollideHullAndCapsule(int capacity, B3Hull hullA, B3Capsule capsuleB, B3Transform transformBtoA) {
        int addr = internal_native_CollideHullAndCapsule_addr(capacity, hullA.native_address, capsuleB.native_address, transformBtoA.native_address);
        if (addr == 0)
            return B3LocalManifold.NULL;
        B3LocalManifold B3LocalManifold_NEW = B3LocalManifold.native_new();
        B3LocalManifold_NEW.internal_reset(addr, true);
        return B3LocalManifold_NEW;
    }

    @org.teavm.jso.JSBody(params = {"capacity", "hullA_addr", "capsuleB_addr", "transformBtoA_addr"}, script = "var returnedJSObj = box3d.B3Collision.prototype.CollideHullAndCapsule(capacity, hullA_addr, capsuleB_addr, transformBtoA_addr);if(!returnedJSObj.hasOwnProperty('ptr')) return 0; return box3d.getPointer(returnedJSObj);")
    public static native int internal_native_CollideHullAndCapsule_addr(int capacity, int hullA_addr, int capsuleB_addr, int transformBtoA_addr);

    public static B3LocalManifold CollideHulls(int capacity, B3Hull hullA, B3Hull hullB, B3Transform transformBtoA) {
        int addr = internal_native_CollideHulls_addr(capacity, hullA.native_address, hullB.native_address, transformBtoA.native_address);
        if (addr == 0)
            return B3LocalManifold.NULL;
        B3LocalManifold B3LocalManifold_NEW = B3LocalManifold.native_new();
        B3LocalManifold_NEW.internal_reset(addr, true);
        return B3LocalManifold_NEW;
    }

    @org.teavm.jso.JSBody(params = {"capacity", "hullA_addr", "hullB_addr", "transformBtoA_addr"}, script = "var returnedJSObj = box3d.B3Collision.prototype.CollideHulls(capacity, hullA_addr, hullB_addr, transformBtoA_addr);if(!returnedJSObj.hasOwnProperty('ptr')) return 0; return box3d.getPointer(returnedJSObj);")
    public static native int internal_native_CollideHulls_addr(int capacity, int hullA_addr, int hullB_addr, int transformBtoA_addr);

    public static B3LocalManifold CollideTriangleAndSphere(int capacity, B3Vec3Array triangleA, B3Sphere sphereB) {
        int addr = internal_native_CollideTriangleAndSphere_addr(capacity, triangleA.native_address, sphereB.native_address);
        if (addr == 0)
            return B3LocalManifold.NULL;
        B3LocalManifold B3LocalManifold_NEW = B3LocalManifold.native_new();
        B3LocalManifold_NEW.internal_reset(addr, true);
        return B3LocalManifold_NEW;
    }

    @org.teavm.jso.JSBody(params = {"capacity", "triangleA_addr", "sphereB_addr"}, script = "var returnedJSObj = box3d.B3Collision.prototype.CollideTriangleAndSphere(capacity, triangleA_addr, sphereB_addr);if(!returnedJSObj.hasOwnProperty('ptr')) return 0; return box3d.getPointer(returnedJSObj);")
    public static native int internal_native_CollideTriangleAndSphere_addr(int capacity, int triangleA_addr, int sphereB_addr);

    public static B3LocalManifold CollideTriangleAndCapsule(int capacity, B3Vec3Array triangleA, B3Capsule capsuleB) {
        int addr = internal_native_CollideTriangleAndCapsule_addr(capacity, triangleA.native_address, capsuleB.native_address);
        if (addr == 0)
            return B3LocalManifold.NULL;
        B3LocalManifold B3LocalManifold_NEW = B3LocalManifold.native_new();
        B3LocalManifold_NEW.internal_reset(addr, true);
        return B3LocalManifold_NEW;
    }

    @org.teavm.jso.JSBody(params = {"capacity", "triangleA_addr", "capsuleB_addr"}, script = "var returnedJSObj = box3d.B3Collision.prototype.CollideTriangleAndCapsule(capacity, triangleA_addr, capsuleB_addr);if(!returnedJSObj.hasOwnProperty('ptr')) return 0; return box3d.getPointer(returnedJSObj);")
    public static native int internal_native_CollideTriangleAndCapsule_addr(int capacity, int triangleA_addr, int capsuleB_addr);

    public static B3LocalManifold CollideTriangleAndHull(int capacity, B3Vec3Array triangleA, int triangleFlags, B3Hull hullB, boolean enableSpeculative) {
        int addr = internal_native_CollideTriangleAndHull_addr(capacity, triangleA.native_address, triangleFlags, hullB.native_address, enableSpeculative);
        if (addr == 0)
            return B3LocalManifold.NULL;
        B3LocalManifold B3LocalManifold_NEW = B3LocalManifold.native_new();
        B3LocalManifold_NEW.internal_reset(addr, true);
        return B3LocalManifold_NEW;
    }

    @org.teavm.jso.JSBody(params = {"capacity", "triangleA_addr", "triangleFlags", "hullB_addr", "enableSpeculative"}, script = "var returnedJSObj = box3d.B3Collision.prototype.CollideTriangleAndHull(capacity, triangleA_addr, triangleFlags, hullB_addr, enableSpeculative);if(!returnedJSObj.hasOwnProperty('ptr')) return 0; return box3d.getPointer(returnedJSObj);")
    public static native int internal_native_CollideTriangleAndHull_addr(int capacity, int triangleA_addr, int triangleFlags, int hullB_addr, boolean enableSpeculative);

    public static B3DistanceOutput ShapeDistance(B3ShapeProxy proxyA, B3ShapeProxy proxyB, B3Transform transformBtoA, boolean useRadii) {
        int addr = internal_native_ShapeDistance_addr(proxyA.native_address, proxyB.native_address, transformBtoA.native_address, useRadii);
        if (addr == 0)
            return B3DistanceOutput.NULL;
        if (B3DistanceOutput_TEMP_STATIC_GEN_0 == null)
            B3DistanceOutput_TEMP_STATIC_GEN_0 = B3DistanceOutput.native_new();
        B3DistanceOutput_TEMP_STATIC_GEN_0.internal_reset(addr, false);
        return B3DistanceOutput_TEMP_STATIC_GEN_0;
    }

    @org.teavm.jso.JSBody(params = {"proxyA_addr", "proxyB_addr", "transformBtoA_addr", "useRadii"}, script = "var returnedJSObj = box3d.B3Collision.prototype.ShapeDistance(proxyA_addr, proxyB_addr, transformBtoA_addr, useRadii);if(!returnedJSObj.hasOwnProperty('ptr')) return 0; return box3d.getPointer(returnedJSObj);")
    public static native int internal_native_ShapeDistance_addr(int proxyA_addr, int proxyB_addr, int transformBtoA_addr, boolean useRadii);

    public static B3CastOutput ShapeCast(B3ShapeProxy proxyA, B3ShapeProxy proxyB, B3Transform transformBtoA, B3Vec3 translationB, float maxFraction, boolean canEncroach) {
        int addr = internal_native_ShapeCast_addr(proxyA.native_address, proxyB.native_address, transformBtoA.native_address, translationB.native_address, maxFraction, canEncroach);
        if (addr == 0)
            return B3CastOutput.NULL;
        if (B3CastOutput_TEMP_STATIC_GEN_0 == null)
            B3CastOutput_TEMP_STATIC_GEN_0 = B3CastOutput.native_new();
        B3CastOutput_TEMP_STATIC_GEN_0.internal_reset(addr, false);
        return B3CastOutput_TEMP_STATIC_GEN_0;
    }

    @org.teavm.jso.JSBody(params = {"proxyA_addr", "proxyB_addr", "transformBtoA_addr", "translationB_addr", "maxFraction", "canEncroach"}, script = "var returnedJSObj = box3d.B3Collision.prototype.ShapeCast(proxyA_addr, proxyB_addr, transformBtoA_addr, translationB_addr, maxFraction, canEncroach);if(!returnedJSObj.hasOwnProperty('ptr')) return 0; return box3d.getPointer(returnedJSObj);")
    public static native int internal_native_ShapeCast_addr(int proxyA_addr, int proxyB_addr, int transformBtoA_addr, int translationB_addr, float maxFraction, boolean canEncroach);

    public static B3TOIOutput TimeOfImpact(B3ShapeProxy proxyA, B3ShapeProxy proxyB, B3Sweep sweepA, B3Sweep sweepB, float maxFraction) {
        int addr = internal_native_TimeOfImpact_addr(proxyA.native_address, proxyB.native_address, sweepA.native_address, sweepB.native_address, maxFraction);
        if (addr == 0)
            return B3TOIOutput.NULL;
        if (B3TOIOutput_TEMP_STATIC_GEN_0 == null)
            B3TOIOutput_TEMP_STATIC_GEN_0 = B3TOIOutput.native_new();
        B3TOIOutput_TEMP_STATIC_GEN_0.internal_reset(addr, false);
        return B3TOIOutput_TEMP_STATIC_GEN_0;
    }

    @org.teavm.jso.JSBody(params = {"proxyA_addr", "proxyB_addr", "sweepA_addr", "sweepB_addr", "maxFraction"}, script = "var returnedJSObj = box3d.B3Collision.prototype.TimeOfImpact(proxyA_addr, proxyB_addr, sweepA_addr, sweepB_addr, maxFraction);if(!returnedJSObj.hasOwnProperty('ptr')) return 0; return box3d.getPointer(returnedJSObj);")
    public static native int internal_native_TimeOfImpact_addr(int proxyA_addr, int proxyB_addr, int sweepA_addr, int sweepB_addr, float maxFraction);

    public static B3PlaneSolverResult SolveMoverPlanes(B3Vec3 targetDelta, B3MoverCollision collision) {
        int addr = internal_native_SolveMoverPlanes_addr(targetDelta.native_address, collision.native_address);
        if (addr == 0)
            return B3PlaneSolverResult.NULL;
        if (B3PlaneSolverResult_TEMP_STATIC_GEN_0 == null)
            B3PlaneSolverResult_TEMP_STATIC_GEN_0 = B3PlaneSolverResult.native_new();
        B3PlaneSolverResult_TEMP_STATIC_GEN_0.internal_reset(addr, false);
        return B3PlaneSolverResult_TEMP_STATIC_GEN_0;
    }

    @org.teavm.jso.JSBody(params = {"targetDelta_addr", "collision_addr"}, script = "var returnedJSObj = box3d.B3Collision.prototype.SolveMoverPlanes(targetDelta_addr, collision_addr);if(!returnedJSObj.hasOwnProperty('ptr')) return 0; return box3d.getPointer(returnedJSObj);")
    public static native int internal_native_SolveMoverPlanes_addr(int targetDelta_addr, int collision_addr);

    public static B3Vec3 ClipVectorToMoverPlanes(B3Vec3 vector, B3MoverCollision collision) {
        int addr = internal_native_ClipVectorToMoverPlanes_addr(vector.native_address, collision.native_address);
        if (addr == 0)
            return B3Vec3.NULL;
        if (B3Vec3_TEMP_STATIC_GEN_0 == null)
            B3Vec3_TEMP_STATIC_GEN_0 = B3Vec3.native_new();
        B3Vec3_TEMP_STATIC_GEN_0.internal_reset(addr, false);
        return B3Vec3_TEMP_STATIC_GEN_0;
    }

    @org.teavm.jso.JSBody(params = {"vector_addr", "collision_addr"}, script = "var returnedJSObj = box3d.B3Collision.prototype.ClipVectorToMoverPlanes(vector_addr, collision_addr);if(!returnedJSObj.hasOwnProperty('ptr')) return 0; return box3d.getPointer(returnedJSObj);")
    public static native int internal_native_ClipVectorToMoverPlanes_addr(int vector_addr, int collision_addr);
}
