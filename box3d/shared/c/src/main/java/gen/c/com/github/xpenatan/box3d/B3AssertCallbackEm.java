/*-------------------------------------------------------
 * This file was generated by JParser
 *
 * Do not make changes to this file
 *-------------------------------------------------------*/

package gen.c.com.github.xpenatan.box3d;

import com.github.xpenatan.jParser.api.NativeObject;

public class B3AssertCallbackEm extends NativeObject {

    private static final java.util.ArrayList<B3AssertCallbackEm> TEAVMC_CALLBACKS = new java.util.ArrayList<>();

    private int teavmcCallbackId = -1;

    private int teavmcRegisterCallback() {
        if (teavmcCallbackId < 0) {
            teavmcCallbackId = TEAVMC_CALLBACKS.size();
            TEAVMC_CALLBACKS.add(this);
        } else {
            TEAVMC_CALLBACKS.set(teavmcCallbackId, this);
        }
        return teavmcCallbackId;
    }

    private static String teavmcCStringToString(org.teavm.interop.Address address) {
        if (address == null || address.toLong() == 0) {
            return null;
        }
        int length = 0;
        while (address.add(length).getByte() != 0) {
            length++;
        }
        char[] chars = new char[length];
        for (int i = 0; i < length; i++) {
            chars[i] = (char) (address.add(i).getByte() & 0xFF);
        }
        return new String(chars);
    }

    private static abstract class TEAVMC_Assert_Function extends org.teavm.interop.Function {

        public abstract boolean call(int callbackId, org.teavm.interop.Address condition_addr, org.teavm.interop.Address fileName_addr, int lineNumber);
    }

    @org.teavm.interop.Export(name = "teavmc_B3AssertCallbackEm_Assert")
    private static boolean teavmc_B3AssertCallbackEm_Assert(int callbackId, org.teavm.interop.Address condition_addr, org.teavm.interop.Address fileName_addr, int lineNumber) {
        return TEAVMC_CALLBACKS.get(callbackId).internal_Assert(teavmcCStringToString(condition_addr), teavmcCStringToString(fileName_addr), lineNumber);
    }

    @org.teavm.interop.Import(name = "teavmc_B3AssertCallbackEm_setupCallback")
    private static native void setupCallback(long this_addr, int callbackId, TEAVMC_Assert_Function Assert_fp);

    static public final B3AssertCallbackEm NULL = B3AssertCallbackEm.native_new();

    /**
     * Dummy constructor, used internally to creates objects without C++ pointer
     */
    @Deprecated()
    protected B3AssertCallbackEm(byte b, char c) {
    }

    /**
     * @return An empty instance without a native address
     */
    public static B3AssertCallbackEm native_new() {
        return new B3AssertCallbackEm((byte) 0, (char) 0);
    }

    protected void deleteNative() {
        internal_native_deleteNative(native_address);
    }

    @org.teavm.interop.Import(name = "com_github_xpenatan_box3d_b3assertcallbackem_deletenative")
    public static native void internal_native_deleteNative(long this_addr);

    public B3AssertCallbackEm() {
        long addr = internal_native_create_addr();
        internal_reset(addr, true);
        setupCallback();
    }

    private void setupCallback() {
        int callbackId = teavmcRegisterCallback();
        setupCallback(native_address, callbackId, org.teavm.interop.Function.get(TEAVMC_Assert_Function.class, B3AssertCallbackEm.class, "teavmc_B3AssertCallbackEm_Assert"));
    }

    protected boolean Assert(String condition, String fileName, int lineNumber) {
        return false;
    }

    private boolean internal_Assert(String condition_addr, String fileName_addr, int lineNumber) {
        return Assert(condition_addr, fileName_addr, lineNumber);
    }

    @org.teavm.interop.Import(name = "com_github_xpenatan_box3d_b3assertcallbackem_create_addr")
    public static native long internal_native_create_addr();
}
