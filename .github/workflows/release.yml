name: Build Release

on:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: maven-release
  cancel-in-progress: false

jobs:
  build-and-upload:
    uses: ./.github/workflows/build_and_upload.yml
    with:
      isRelease: true
      shouldUpload: true
    secrets: inherit
